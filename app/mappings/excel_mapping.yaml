fields:
  request_date: { named_range: request_date, format: date }
  normal_due_date: { named_range: normal_due_date, format: date }
  requester_department: { named_range: requester_department }
  requester_manager: { named_range: requester_manager }
  requester_staff: { named_range: requester_staff }

  project_name: { named_range: project_name }
  activity_purpose: { named_range: activity_purpose }
  activity_start: { named_range: activity_start }

  project_target_item: { named_range: project_target_item }

  counterparty_name: { named_range: counterparty_name }
  counterparty_address: { named_range: counterparty_address }
  counterparty_profile: { named_range: counterparty_profile }
  counterparty_type: { named_range: counterparty_type }

  contract_form: { named_range: contract_form }
  related_contract_flag: { named_range: related_contract_flag }

  amount_jpy: { named_range: amount_jpy, format: currency_jpy }

  info_from_us: { named_range: info_from_us }
  info_from_us_other: { named_range: info_from_us_other }
  info_from_them: { named_range: info_from_them }
  info_from_them_other: { named_range: info_from_them_other }

  our_overall_summary: { named_range: our_overall_summary }
  their_overall_summary: { named_range: their_overall_summary }

  desired_contract: { named_range: desired_contract }
